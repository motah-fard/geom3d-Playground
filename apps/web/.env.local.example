# Copy this file to .env.local and adjust if your backend runs somewhere
# other than the default (see services/api's PORT env var / default 8081).
#
# The frontend falls back to http://localhost:8081 if this isn't set, so
# this file is only needed if you're running the backend elsewhere.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8081
